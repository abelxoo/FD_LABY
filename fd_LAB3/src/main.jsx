import React from "react";
import ReactDOM from "react-dom/client";
import DataDisplayApp from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DataDisplayApp />
  </React.StrictMode>
);
